# MacDown for Windows v0.2.0 - Phase 3 完成 🎉

> 現代化的 Markdown 編輯器，具備強大的 View 與 UI 功能

## ✨ 主要新增功能

### 🎛️ Phase 3 核心功能

#### Toggle Toolbar（切換工具列）
- **快捷鍵**: `Ctrl+Shift+T`
- **功能**: 一鍵隱藏/顯示編輯器工具列
- **用途**: 提供更大的編輯空間，適合專注寫作時使用

#### Copy Rendered HTML（複製渲染後的 HTML）
- **快捷鍵**: `Ctrl+Shift+C`
- **功能**: 一鍵複製 Markdown 渲染後的 HTML 內容到剪貼簿
- **用途**: 方便將內容貼到其他支援 HTML 的地方（如郵件、部落格等）

---

## 🎨 編輯器增強

### Markdown 格式化工具列
新增包含 11 個按鈕的工具列，支援：
- **粗體** (Ctrl+B) / **斜體** (Ctrl+I)
- `程式碼` / > 引用
- 無序列表 / 有序列表
- 標題 (H1-H6)
- 超連結插入（自動選中 URL）
- 圖片插入（自動選中圖片 URL）

### Block 操作
- **縮排** (Tab)
- **反縮排** (Shift+Tab)
- **Convert To**: 標題、有序列表、無序列表之間轉換

---

## 👀 預覽功能強化

### 預覽視窗縮放
- 縮放範圍：50% ~ 200%
- 預設比例：80%（最適合觀看）
- 縮放控制條位於預覽區域頂部

### 同步滾動
- 編輯器滾動時，預覽視窗自動同步
- 使用 debounce 優化滾動效能

### Split View 分割比例
- **Left 1:1 Right** (Ctrl+1)
- **Left 3:1 Right** (Ctrl+2)
- **Left 1:3 Right** (Ctrl+3) ← 預設比例

---

## 📋 View 選單改進

### 動態視圖控制
- **Hide/Show Editor** (Ctrl+Shift+E)
- **Hide/Show Preview** (Ctrl+Shift+P)
- 選單文字會根據當前狀態即時更新

---

## 📂 檔案管理增強

### Open Recent（最近檔案）
- 自動記錄最近開啟的檔案
- 持久化儲存（重啟應用程式後仍可用）
- 動態更新最近檔案列表
- 一鍵清除最近檔案記錄

### 檔案操作完善
- **File > New** (Ctrl+N): 開啟空白檔案
- **File > Close** (Ctrl+W): 關閉當前檔案
- 關閉前檢查未儲存變更並提示

---

## 🖨️ 列印功能

### Page Setup（頁面設定）
- 開啟列印對話框進行頁面設定
- 支援紙張大小、方向、邊距等設定

### Print（列印）
- **快捷鍵**: `Ctrl+P`
- 正確列印 Markdown 預覽內容
- 支援完整的 Markdown 樣式（標題、程式碼、表格等）
- 在隱藏視窗中渲染 HTML 後進行列印

---

## 📤 匯出功能改進

### 使用者體驗優化
- HTML/PDF 匯出後**自動開啟資料夾並選中檔案**
- 移除匯出成功的 alert 提示
- 改用直接開啟資料夾作為反饋
- 保留錯誤提示，匯出失敗時顯示清楚的錯誤訊息

---

## 🐛 Bug 修正

### 視窗管理
- ✅ 修正視窗右上角 X 按鈕無法關閉問題
- ✅ 關閉視窗前檢查未儲存的變更
- ✅ 提供儲存選項後再關閉

### UI 改進
- ✅ File/New 現在開啟空白檔案而非歡迎訊息
- ✅ 修正 TypeScript 編譯錯誤
- ✅ 修正開發環境 port 不一致問題（5173 vs 5174）

---

## ⌨️ 完整快捷鍵列表

| 功能 | 快捷鍵 |
|------|--------|
| 新增檔案 | `Ctrl+N` |
| 開啟檔案 | `Ctrl+O` |
| 關閉檔案 | `Ctrl+W` |
| 儲存檔案 | `Ctrl+S` |
| 另存新檔 | `Ctrl+Shift+S` |
| 匯出 HTML | `Ctrl+Shift+H` |
| 匯出 PDF | `Ctrl+Shift+P` |
| 列印 | `Ctrl+P` |
| 復原/重做 | `Ctrl+Z` / `Ctrl+Y` |
| 複製 HTML | `Ctrl+Shift+C` |
| 切換工具列 | `Ctrl+Shift+T` |
| 隱藏編輯器 | `Ctrl+Shift+E` |
| 隱藏預覽 | `Ctrl+Shift+P` |
| 分割比例 1:1 | `Ctrl+1` |
| 分割比例 3:1 | `Ctrl+2` |
| 分割比例 1:3 | `Ctrl+3` |

---

## 📚 技術改進

### 新增模組
- `print-handler.ts` - 列印功能處理器
- `recentFiles.ts` - 最近檔案管理（main & renderer）
- `Toolbar.tsx` - Markdown 格式化工具列
- `ZoomControl.tsx` - 預覽縮放控制元件

### IPC 通訊增強
- 新增 20+ 個 IPC 事件處理器
- 改進視圖狀態同步機制
- 加強視窗關閉流程控制

### 文檔完善
- 新增 `CHANGELOG.md` 記錄所有變更
- 更新 `README.md` 功能說明與快捷鍵表
- 按開發階段組織變更記錄（Phase 1-4）

---

## 🎯 下一步計劃

### Phase 4 - 進階功能（規劃中）
- ❌ 搜尋與取代
- ❌ 拼字檢查
- ✅ HTML/PDF 匯出（已完成）
- ✅ 列印功能（已完成）

---

## 📦 安裝與使用

### 系統需求
- **作業系統**: Windows 10/11
- **Node.js**: >= 18.0.0
- **pnpm**: >= 8.0.0

### 快速開始

```bash
# 1. Clone 專案
git clone https://github.com/code-nomad365/macdown-win.git
cd macdown-win

# 2. 安裝依賴
pnpm install

# 3. 重新編譯原生模組
pnpm --filter @macdown/main rebuild

# 4. 編譯專案
pnpm build

# 5. 啟動應用程式
pnpm start

# 或使用一鍵啟動
start.bat
```

---

## 🙏 致謝

感謝所有使用 MacDown for Windows 的用戶！

如果這個專案對您有幫助，請給我們一個 ⭐ Star！

---

## 📮 回饋與問題

- **GitHub Issues**: [提交問題](https://github.com/code-nomad365/macdown-win/issues)
- **GitHub Repository**: [code-nomad365/macdown-win](https://github.com/code-nomad365/macdown-win)

---

**Made with ❤️ using Electron + React + TypeScript**

🤖 Generated with [Claude Code](https://claude.com/claude-code)
